{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-freeglut",
      "type": "shell",
      "command": "g++",
      "args": ["main.cpp", "-o", "app", "-lGL", "-lGLU", "-lglut"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}

// To build

// ctrl + shift + B
// OR gcc++ main.cpp -o app -lGL -lGLU -lglut

// To run

// ./app
